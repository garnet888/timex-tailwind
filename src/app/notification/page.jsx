import Notification from './Notification';

const title = 'Мэдэгдэл';
export const metadata = { title };

const NotificationPage = () => {
  return <Notification title={title} />;
};

export default NotificationPage;
