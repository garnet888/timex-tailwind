import Settings from './Settings';

const title = 'Байгууллагын тохиргоо';
export const metadata = { title };

const SettingsPage = () => {
  return <Settings title={title} />;
};

export default SettingsPage;
