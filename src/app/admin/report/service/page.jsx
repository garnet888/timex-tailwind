import Service from './Service';

const title = 'Топ үйлчилгээ';
export const metadata = { title };

const ServicePage = () => {
  return <Service title={title} />;
};

export default ServicePage;
