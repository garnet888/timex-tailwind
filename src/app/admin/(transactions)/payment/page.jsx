import Payment from './Payment';

const title = 'Үйлчилгээний эрх';
export const metadata = { title };

const PaymentPage = () => {
  return <Payment title={title} />;
};

export default PaymentPage;
