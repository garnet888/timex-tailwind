import AppUser from './AppUser';

const title = 'Хэрэглэгч';
export const metadata = { title };

const AppUserPage = () => {
  return <AppUser title={title} />;
};

export default AppUserPage;
