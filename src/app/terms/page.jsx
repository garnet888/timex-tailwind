import RenderTerms from './RenderTerms';

const title = 'Үйлчилгээний нөхцөл';

export const metadata = { title };

const Terms = () => {
  return <RenderTerms title={title} />;
};

export default Terms;
