import Customers from './Customers';

const title = 'Үйлчлүүлэгчид';
export const metadata = { title };

const CustomersPage = () => {
  return <Customers title={title} />;
};

export default CustomersPage;
